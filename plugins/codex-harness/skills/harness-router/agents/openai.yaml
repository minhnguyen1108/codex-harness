interface:
  display_name: "Harness Router"
  short_description: "Route coding tasks through the right workflow"
  default_prompt: "Use $harness-router to route and execute this coding task safely."

policy:
  allow_implicit_invocation: true
